### euclidian prox funtion on logistic datasets

## iter vs objective gap plots (trajectories) objective gap x hat only

SDA simple , weighted, subgradient ; , gamma mult = 1, labmda = 1, seed=0, all D, separate plots for datasets

SDA simple , weighted, subgradient ; , all gamma, labmda = 1, seed=0, D==32, separate plots for datasets

% grid for runtime vs final objective gap
runtime vs final objective gap -same as is
