# Lasso Logistic SDA vs SSA Comparison

Compares weighted simple dual averaging against stochastic simple averages on the
same lasso logistic datasets used by `lasso_logistic_sparsity`.

`sklearn_saga` runs are included only as empirical reference rows for objective-gap
plots.
