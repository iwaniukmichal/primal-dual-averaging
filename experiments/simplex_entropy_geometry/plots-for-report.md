### entropy prox on benchmark functions

## iter vs objective gap plots (trajectories) objective_gap_x_hat only

SDA simple , weighted, gammamult = [0.1,0.5,1], unrestricted, objective_gap_x_hat , D=[2,3], all objectives ; diff methods diff gammas and diff D on one plot , separate plots for each objective

% grid of all subgrad params, sda simple and weighter for last plot

runtime vs final objective gap -same as is
